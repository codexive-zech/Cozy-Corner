const SectionTitle = ({ title }) => {
  return (
    <div>
      <h1 className=" font-bold text-4xl capitalize border-b border-base-300 pb-4 tracking-wider ">
        {title}
      </h1>
    </div>
  );
};

export default SectionTitle;
